# Schema generated by Grats (https://grats.capt.dev)
# Do not manually edit. Regenerate by running `npx grats`.

input SiteDiaryInput {
  attachments: [String!]
  attendees: [String!]
  content: String
  createdBy: String!
  date: String!
  id: String
  title: String!
  weather: WeatherInput
}

input WeatherInput {
  description: String!
  temperature: Int!
}

type AIBeautifyResult {
  beautifiedText: String!
  enhanced: Boolean!
  originalText: String!
}

type AISummaryResult {
  diariesCount: Int!
  endDate: String!
  startDate: String!
  summary: String!
}

type Mutation {
  """Enhance user-entered text using AI to make it more professional"""
  beautifyTextMutation(text: String!): AIBeautifyResult!
  createSiteDiary(input: SiteDiaryInput!): SiteDiary!
  deleteSiteDiary(id: String!): Boolean!
  """Summarize site diaries within a date range using AI"""
  summarizeSiteDiaries(endDate: String!, limit: Int, startDate: String!): AISummaryResult!
  updateSiteDiary(id: String!, input: SiteDiaryInput!): SiteDiary
}

type Query {
  siteDiaries: [SiteDiary!]!
  """Get site diaries within a date range"""
  siteDiariesByDateRange(endDate: String!, startDate: String!): [SiteDiary!]!
  siteDiary(id: String!): SiteDiary
}

type SiteDiary {
  attachments: [String!]
  attendees: [String!]
  content: String
  createdBy: String!
  date: String!
  id: String!
  title: String!
  weather: Weather
}

type Weather {
  description: String!
  temperature: Int!
}
